{
  "general": {
    "description": "Every configured MCP server, no shell. What you get with no profiles file at all.",
    "mcp_servers": null
  },
  "repository": {
    "description": "Read a checked-out project and its history. No interpreters: see the note in docs/PROFILES.md.",
    "system_prompt": "You are working inside a git repository. Read before you write, and say plainly when you cannot verify something rather than assuming it.",
    "mcp_servers": ["git", "filesystem"],
    "shell": {
      "enabled": true,
      "policy": "docker",
      "workspace_root": "/workspace",
      "allow": ["git", "ls", "cat", "head", "tail", "wc", "rg", "diff"],
      "approve": ["git push", "git reset", "git clean", "git checkout"]
    },
    "limits": { "tool_calls_per_run": 40, "model_calls_per_run": 25 }
  },
  "analysis": {
    "description": "Read data files and summarise them. Reading only — computing over them needs an interpreter, which this deliberately does not have.",
    "system_prompt": "You are examining data. State the shape and provenance of what you read before drawing conclusions from it.",
    "mcp_servers": ["filesystem"],
    "shell": {
      "enabled": true,
      "policy": "docker",
      "workspace_root": "/workspace",
      "allow": ["ls", "cat", "head", "tail", "wc", "cut", "sort", "uniq", "rg"],
      "approve": []
    },
    "limits": { "tool_calls_per_run": 60, "model_calls_per_run": 30 }
  },
  "research": {
    "description": "Search and read. Deliberately no shell — that pair is the injection path 0.2.0 removed.",
    "system_prompt": "You are researching. Cite what you read, and say plainly when a source does not answer the question.",
    "mcp_servers": ["search", "fetch"],
    "shell": { "enabled": false },
    "limits": { "tool_calls_per_run": 30, "model_calls_per_run": 20 }
  }
}
